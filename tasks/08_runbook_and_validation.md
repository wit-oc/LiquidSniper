# Task 08 — Runbook + validation

## Goal

Make Phase 1 easy to run and verify.

## Deliverables

- Update/extend `specs/08_testing_runbook.md` into an ops runbook (or new `RUNBOOK.md`)
- Add automated checks:
  - `pytest` in CI
  - basic lint/format (optional)

## Acceptance criteria

- A new contributor can get to a working system using only the runbook
